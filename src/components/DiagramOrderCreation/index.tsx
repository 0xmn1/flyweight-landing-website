import OrderCreation from './order-creation.png';
import React from 'react';
import styles from './DiagramOrderCreation.module.scss';

const DiagramOrderCreation = (props: unknown) => (
  <img src={OrderCreation} alt="diagram of order creation" />
);

export default DiagramOrderCreation;
